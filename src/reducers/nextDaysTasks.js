const defaultState = {
    loading: false
};

const nextDaysTasks = (state = defaultState, action) => {
    switch (action.type) {
        default:
            return state;
    }
};

export default nextDaysTasks;