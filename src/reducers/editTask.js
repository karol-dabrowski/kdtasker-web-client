const defaultState = {
    loading: false
};

const editTask = (state = defaultState, action) => {
    switch (action.type) {
        default:
            return state;
    }
};

export default editTask;